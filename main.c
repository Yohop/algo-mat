#include <stdio.h>

// rm -rf * && rm -rf .* && git clone https://github.com/Yohop/algo-mat.git .

void print_array(int arr[], int n)
{
    for (int i = 0; i < n; i++)
        printf("%d ", arr[i]);
    printf("\n");
}

int main()
{
    // printf("Hello, World!");
   
    return 0;
}
